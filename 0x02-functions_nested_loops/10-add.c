#include "main.h"

/**
 * add -- adds two integers and returns the result
 * @a: first integer argument
 * @b: second integer argument
 * Return: the addition of two integer values
 */
int add(int a, int b)
{
	return (a + b);
}
